# hupoker Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-06

## Active Technologies

- Rust 1.78 or later (stable channel) + `rand_core` + `getrandom` + `rand_chacha` (RNG), `tokio` (async runtime), `serde` + `serde_json` (serialization), `egui` + `eframe` (UI), `tracing` (logging), `thiserror`/`anyhow` (errors), `toml` (config), `chacha20poly1305` (encryption). (001-heads-up-poker-mvp)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style

Rust 1.78 or later (stable channel): Follow standard conventions

## Recent Changes

- 001-heads-up-poker-mvp: Added Rust 1.78 or later (stable channel) + `rand_core` + `getrandom` + `rand_chacha` (RNG), `tokio` (async runtime), `serde` + `serde_json` (serialization), `egui` + `eframe` (UI), `tracing` (logging), `thiserror`/`anyhow` (errors), `toml` (config), `chacha20poly1305` (encryption).

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
